# NoteTaker-Java-Hibernate-JSP-Servlet
Created the mini project(Note Taker: it will take the notes and store in database ) based on Java Framework Hibernate and also used the JSP and servlet for the same 
